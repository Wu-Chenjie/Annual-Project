# 后续代码实施映射

> 编制日期：2026-05-05  
> 关联文档：
> - `docs/后续SCI文献调研与改进计划书.md` —— 文献对标、标准转化、数据结构与接口建议
> - `docs/项目总改进规划.md` —— 实施路线、优先级、里程碑与工时（2026-05-03）
> - `docs/优化反效果归因分析.md` —— R1-R8 避坑准则与合入流程

本文档是 `后续SCI文献调研与改进计划书.md` 的工程落地映射。它不重复文献动机和理论推导，只保留「新增什么文件、改什么接口、怎么回退、怎么验收」的代码层细节。

---

## 一、总体目标

将 `后续SCI文献调研与改进计划书.md` 的 S1-S5 增量转化为可编译、可测试、可回退的代码变更。

| 增量代号 | 文献/标准来源 | 核心文件 | 与 项目总改进规划.md 的对接 |
|---|---|---|---|
| S1 | Mellinger & Kumar ICRA 2011; Zhang et al. IJCAS 2024 | `core/planning/trajectory_optimizer.py` | P1-1 FIRI 多段走廊轨迹优化 |
| S2 | Faessler et al. RA-L 2018; 系统辨识文献 | `core/drone_params.py` | P1 之后参数可解释性补强 |
| S3 | Lee et al. CDC 2010 | `core/controller.py` 扩展 | P1-3 MPC 评估后的对照项 |
| S4 | Hoenig et al. T-RO 2018; Park et al. RA-L 2022 | `core/formation_safety.py` | P2-2 动态分区策略延伸 |
| S5 | T/CICC 27002/27006/27007 | `core/safety_profiles.py`, `core/risk_report.py` | **新增维度** |

---

## 二、当前代码基础

远程仓库已有基础（`项目总改进规划.md` 第1节基线）：

- `next_project/core/planning/`：A*、Hybrid A*、D* Lite、RRT*、Informed RRT*、ESDF、FIRI。
- `next_project/simulations/obstacle_scenario.py`：障碍物、在线重规划、APF、GNN/Danger 调度、容错检测。
- `next_project/simulations/formation_simulation.py`：`SimulationConfig`，配置项较多，后续应做分组和 profile 化。
- `next_project/simulations/benchmark.py`：基础评测，需扩展为 SCI 级多场景、多指标、消融实验。

---

## 三、S0：实验基线与指标提取（前置工作）

**注意**：此工作不创造新算法，只建立评估系统。`项目总改进规划.md` P0 可靠性补强已基本完成，本阶段是补全 SCI 级指标。

### 3.1 新增文件

```text
next_project/experiments/
├── scenario_registry.py
├── metrics_extractor.py
├── run_sci_baseline.py
├── run_ablation.py
└── report_writer.py
```

### 3.2 核心实现

**`scenario_registry.py`**
- 统一注册：`warehouse_online`、`school_corridor_online`、`company_cubicles_online`、`meeting_room_online`、`laboratory_online`。

**`metrics_extractor.py`**
- 从仿真结果中提取：成功率、碰撞数、最小 clearance、重规划次数、路径长度、速度、加速度、jerk、tracking RMS、replan latency。

**`run_sci_baseline.py`**
- 一键跑所有标准场景，输出 CSV/JSON。

**`run_ablation.py`**
- 支持关闭 ESDF、FIRI、APF、GNN、formation envelope 等模块，做消融实验。

**`report_writer.py`**
- 把实验结果导出为 Markdown 报告。

### 3.3 输出

```text
outputs/sci_baseline/summary.csv
outputs/sci_baseline/summary.json
outputs/reports/*.md
```

### 3.4 验收

- 五个在线场景可一键跑出指标。
- 关闭任一模块后指标变化可被量化记录。
- **回退**：删除 `experiments/` 目录不影响任何仿真主流程。

---

## 四、S1：连续轨迹优化模块

### 4.1 新增文件

```text
next_project/core/planning/trajectory_optimizer.py
next_project/core/planning/trajectory_types.py
tests/test_trajectory_optimizer.py
```

### 4.2 核心接口

```python
# core/planning/trajectory_types.py
@dataclass
class TrajectorySample:
    t: float
    pos: np.ndarray
    vel: np.ndarray
    acc: np.ndarray
    yaw: float = 0.0

@dataclass
class TrajectoryResult:
    samples: list[TrajectorySample]
    success: bool
    reason: str = ""
    cost_smooth: float = 0.0
    min_clearance: float = float("inf")
    max_vel: float = 0.0
    max_acc: float = 0.0
    solve_time_ms: float = 0.0

# core/planning/trajectory_optimizer.py
class TrajectoryOptimizer:
    def optimize(
        self,
        path: np.ndarray,
        corridor: list[FIRICorridor] | None = None,
        max_vel: float = 2.0,
        max_acc: float = 2.0,
    ) -> TrajectoryResult:
        ...
```

### 4.3 技术路线

第一版采用 **minimum jerk / minimum snap 多项式** 或 **uniform B-spline**，不立刻引入重优化依赖。输入为当前 A*/FIRI 输出路径，输出为带时间参数的连续轨迹。

### 4.4 接入点

- `ObstacleScenarioSimulation._plan_offline()`：离线路径生成后可选调用。
- `WindowReplanner` 输出路径后：在线局部窗口内可选调用。
- `Controller.compute_control(..., target_vel, target_acc ...)`：控制器需扩展前馈接口。

### 4.5 回退策略

```text
FIRI corridor -> trajectory optimization success -> use optimized trajectory
FIRI corridor -> trajectory optimization fail -> use current FIRI refined path
FIRI refined path unsafe -> fallback segment planner
fallback unsafe -> reject path and log clearance_blocked
```

### 4.6 验收指标

| 指标 | 目标 |
|---|---|
| 碰撞数 | 保持 0 |
| jerk 代价 | 相比基线下降 20%-40% |
| leader tracking RMS | 不高于当前基线 |
| 轨迹采样点 clearance | 全部通过 SDF 检查 |
| replan latency p95 | 在线模式不超过基线 1.5 倍 |

---

## 五、S2：DroneParams 参数 profile

### 5.1 新增文件

```text
next_project/core/drone_params.py
tests/test_drone_params.py
```

### 5.2 核心设计

```python
# core/drone_params.py
@dataclass
class DroneParams:
    name: str
    mass: float
    inertia: np.ndarray
    arm_length: float
    rotor_kf: float
    rotor_km: float
    motor_tau: float
    omega_max: float
    drag_linear: np.ndarray
    drag_quadratic: np.ndarray
    rotor_drag: np.ndarray

    def __post_init__(self):
        assert self.max_total_thrust > self.mass * 9.81 * 1.3
        assert self.hover_omega < self.omega_max
        assert np.all(np.linalg.eigvals(self.inertia) > 0)

    @property
    def max_total_thrust(self) -> float:
        return 4.0 * self.rotor_kf * self.omega_max ** 2

    @property
    def hover_omega(self) -> float:
        return np.sqrt(self.mass * 9.81 / (4.0 * self.rotor_kf))

# 注册表
DRONE_PROFILES: dict[str, DroneParams] = {
    "default_1kg": DroneParams(...),
    "indoor_micro": DroneParams(...),
    "light_uav": DroneParams(...),
}
```

### 5.3 修改点

| 文件 | 改动 |
|---|---|
| `core/drone.py` | 构造函数接收 `DroneParams`，默认 `default_1kg` |
| `core/rotor.py` | 从 profile 读取推力/力矩参数 |
| `core/allocator.py` | 与 profile 保持一致 |
| `config.py` | 增加 `drone_profile: str = "default_1kg"` |

### 5.4 验收

| 检查项 | 通过标准 |
|---|---|
| 默认 profile 回归 | 与当前 baseline 指标差异 < 1% |
| 悬停测试 | 位置误差不发散 |
| profile 切换 | 不需要修改算法源码 |
| 参数一致性 | `max_total_thrust > mass * g * margin` |
| rotor 与 allocator | 参数来源同一 profile |

---

## 六、S3：轨迹跟踪控制器扩展

### 6.1 修改文件

```text
next_project/core/controller.py
```

### 6.2 统一接口

```python
def compute_control(
    self,
    state,
    target_pos,
    target_vel=None,
    target_acc=None,
    target_yaw=0.0,
    target_yaw_rate=0.0,
) -> np.ndarray:
    ...
```

### 6.3 新增可选控制器

```python
class GeometricSE3Controller:
    """
    在 SE(3) 上计算姿态误差，避免 Euler 角奇异。
    仅作为实验 profile，不替换默认控制器。
    """
    def compute_control(self, state, target_pos, target_vel, target_acc,
                        target_yaw, target_yaw_rate) -> np.ndarray:
        e_R = 0.5 * vee(R_d.T @ R - R.T @ R_d)
        e_omega = omega - R.T @ R_d @ omega_d
        ...
```

### 6.4 配置

```python
controller_kind: str = "pid_smc"
# 可选: "pid_smc" | "backstepping_smc" | "se3_geometric"
```

### 6.5 验收

| 检查项 | 通过标准 |
|---|---|
| 无障碍场景 | 不发散 |
| 高曲率轨迹 | tracking RMS 降低 |
| 推力饱和 | 不显著增加 |
| 默认场景 | 不引入回归 |

---

## 七、S4：多机安全走廊与冲突消解

### 7.1 新增/扩展文件

```text
next_project/core/formation_safety.py
tests/test_formation_safety.py
```

### 7.2 当前代码状态

`core/topology.py` 已有 `auto_shrink()` 和 `fault_reconfigure()`，但存在以下问题：

| 问题 | 位置 | 后果 |
|---|---|---|
| `auto_shrink()` 未被主流程调用 | 有定义（第178行），`obstacle_scenario.py` 无调用 | 队形不会根据通道宽度自动收缩 |
| `envelope_radius()` 使用各向同性范数 | `core/topology.py` 第149行 `max(||offset||)` | 线性队形横向包络被高估为 18.2 m，仓库栅格膨胀 18.7 m，A* 失败 |
| `_inflate_r()` 依赖错误包络 | `obstacle_scenario.py` 第397行 | 全局栅格过度膨胀，窄通道被堵死 |

**修复文件清单**：

| 文件 | 修改内容 |
|---|---|
| `core/topology.py` | 新增 `envelope_per_axis()`；`envelope_radius()` 标记 deprecated |
| `simulations/obstacle_scenario.py` | `_inflate_r()` 改用分轴包络；接入 `auto_shrink()` |
| `core/obstacles.py` | `OccupancyGrid.inflate()` 支持三轴膨胀 `(rx, ry, rz)` |
| `tests/test_obstacle_scenario.py` | 增加分轴包络回归测试 |

### 7.3 实现内容（修复后）

| 功能 | 说明 |
|---|---|
| `formation_envelope_per_axis()` | 分轴包络 (lateral, longitudinal, vertical) |
| `min_inter_drone_distance()` | 机间最小距离实时检查 |
| `downwash_zone()` | 下洗保护区圆柱建模 |
| `follower_safety_correction()` | follower 目标点沿 leader 方向收缩 |
| `narrow_channel_shrink()` | 窄通道自动触发 compact/line 队形 |

### 7.3 核心逻辑

```python
def narrow_channel_shrink(corridor_width, formation_lateral_radius):
    if corridor_width < 2 * formation_lateral_radius:
        switch_to_compact_formation()
```

### 7.4 关键约束

- **R7 合规**：包络计算必须使用分轴，**禁用** `max(||offset||)`。
- **R1 合规**：队形包络是局部队形属性，禁止用全局栅格膨胀实现。

### 7.5 指标

| 指标 | 目标 |
|---|---|
| `min_inter_drone_distance` | 不低于配置阈值 |
| `follower_collision` | 保持 0 |
| 队形误差峰值 | 不因收缩策略显著增大 |
| 窄通道完成率 | 高于当前基线 |

---

## 八、S5：Safety Profile 与风险报告

### 8.1 新增文件

```text
next_project/core/safety_profiles.py
next_project/core/risk_report.py
tests/test_safety_profiles.py
tests/test_risk_report.py
```

### 8.2 Profile 定义

```python
@dataclass
class SafetyProfile:
    name: str
    horizontal_position_error: float
    vertical_position_error: float
    surveillance_update_period: float
    horizontal_hold_spacing: float
    vertical_hold_spacing: float
    decision_time: float
    horizontal_avoid_time: float
    vertical_avoid_time: float

SAFETY_PROFILES = {
    "indoor_demo": SafetyProfile(...),
    "indoor_research": SafetyProfile(...),
    "low_altitude_light_uav": SafetyProfile(...),
}
```

### 8.3 风险报告输出

```json
{
  "safety_profile": "indoor_research",
  "risk_level": "low",
  "events": [
    {
      "type": "clearance_blocked",
      "t": 24.4,
      "frame": 62,
      "position": [6.2, 3.1, 2.5],
      "min_clearance": 0.18,
      "threshold": 0.30,
      "reason": "no_continuous_clearance_path"
    }
  ],
  "summary": {
    "collision_count": 0,
    "clearance_violation_count": 0,
    "inter_drone_violation_count": 0,
    "restricted_airspace_violation_count": 0
  }
}
```

### 8.4 验收

- 室内 profile 不改变当前地图尺度。
- 低空标准 profile 能输出符合 T/CICC 27007 口径的安全间隔计算说明。
- 风险报告能用于答辩说明 CONOPS、危险识别和剩余风险。

---

## 九、S6：报告与回放增强

### 9.1 扩展文件

```text
next_project/simulations/visualization.py
next_project/visualization/
next_project/outputs/reports/
```

### 9.2 新增图表

| 图表 | 内容 |
|---|---|
| clearance-time 曲线 | 轨迹到障碍物的 signed distance 时序 |
| speed / acceleration / jerk 曲线 | 轨迹动力学质量 |
| replan latency 分布 | 在线重规划耗时统计 |
| inter-drone distance 曲线 | 多机间距时序 |
| collision / blocked / risk event timeline | 风险事件时间轴 |

### 9.3 最终输出

```text
outputs/reports/{scenario}_{timestamp}.md
outputs/reports/{scenario}_{timestamp}.json
outputs/figures/*.png
```

---

## 十、与现有文档的衔接

| 本文档章节 | 理论来源 | 代码落点 | 回退开关 |
|---|---|---|---|
| S0 基线 | `项目总改进规划.md` P0 已完成 | `experiments/` | 删除目录即可 |
| S1 轨迹优化 | `后续SCI文献调研与改进计划书.md` 5.2 | `core/planning/trajectory_optimizer.py` | `trajectory_optimizer_enabled=False` |
| S2 参数 profile | `后续SCI文献调研与改进计划书.md` 5.4 | `core/drone_params.py` | `drone_profile="default_1kg"` |
| S3 控制器 | `后续SCI文献调研与改进计划书.md` 5.3 | `core/controller.py` | `controller_kind="pid_smc"` |
| S4 多机安全 | `后续SCI文献调研与改进计划书.md` 5.5 | `core/formation_safety.py` | `formation_safety_enabled=False` |
| S5 标准转化 | `后续SCI文献调研与改进计划书.md` 5.6 | `core/safety_profiles.py` | `safety_profile="indoor_demo"` |
| S6 报告增强 | `后续SCI文献调研与改进计划书.md` P6 | `simulations/visualization.py` | 不影响主流程 |

---

## 十一、验证命令

### 11.1 已有回归（每次改动必跑）

```powershell
python -m pytest "tests\test_obstacle_scenario.py" -q -k "firi or obstacle_simulation_zero_collision or hybrid_astar_obstacle"
python -m pytest "tests\test_replanner_semantics.py" "tests\test_replanner_subgoal.py" -q
python -m pytest "tests\test_esdf_correct.py" -q
python -m pytest "tests\test_gnn_planner.py" -q
python -m pytest "tests\test_fault_tolerance_online.py" -q
```

### 11.2 新增后补充

```powershell
python -m pytest "tests\test_trajectory_optimizer.py" -q
python -m pytest "tests\test_drone_params.py" -q
python -m pytest "tests\test_safety_profiles.py" -q
python -m pytest "tests\test_risk_report.py" -q
python -m pytest "tests\test_formation_safety.py" -q
```

---

## 十二、优先级建议

与 `项目总改进规划.md` 和 `后续SCI文献调研与改进计划书.md` 对齐：

**短期优先**（第一批）：
1. S0 实验基线与指标提取。
2. S1 连续轨迹优化原型。
3. S2 DroneParams profile。
4. S5 safety profile 与风险报告。

**中期推进**（第二批）：
1. S3 SE(3) 控制器对照。
2. S4 多机安全走廊。
3. S6 自动报告和回放增强。

**暂缓**：
- 不立即把默认控制器换成 MPC/MPCC。
- 不立即引入强化学习作为主规划器。
- 不一次性合并所有论文方法。
- 不把低空标准安全间隔直接套入室内地图。

---

## 十三、项目功能全景与已知缺陷登记

本节登记除 S1-S6 之外的其他功能模块的已知缺陷和改进需求，作为代码基线的全景视图。

### 13.1 功能全景概览

| 模块 | Python | C++ | 关键缺陷 |
|---|---|---|---|
| **路径规划** |
| A* | ✅ | ✅ | |
| Hybrid A* | ✅ | ✅ | |
| Dijkstra | ✅ | ✅ | |
| RRT* / Informed RRT* | ✅ | ✅ | |
| D* Lite | ✅ | ✅ | |
| ESDF 软代价 | ✅ | ✅ | `compute_esdf()` EDT 输入语义已修正（2026-05-03） |
| FIRI 凸走廊 | ✅ | ✅ | 尚未生成时间参数化轨迹 |
| GNN 可见图 + Danger 调度 | ✅ | ⚠️ | C++ 第四阶段同步， Danger 调度可能不完整 |
| **在线重规划** |
| WindowReplanner 三级分层 | ✅ | ⚠️ 简化 | C++ 为简化版，无完整 sliding-window A* |
| 六向距离传感器 | ✅ | ✅ | |
| 动态障碍 ADD/REMOVE | ✅ | ✅ | |
| 传感器 TTL 语义层 | ✅ | ✅ | |
| APF 避障三档 profile | ✅ | ✅ | aggressive 档仅用于对比实验 |
| **控制器** |
| PID 串级 | ✅ | ✅ | |
| Backstepping | ✅ | ✅ | |
| SMC 滑模 | ✅ | ✅ | |
| 倾角限幅 + 推力一致性 | ✅ | ✅ | |
| SE(3) 几何控制 | ❌ | ❌ | 未实现 |
| **编队** |
| 固定队形偏移 | ✅ | ✅ | |
| 队形平滑切换 | ✅ | ✅ | |
| 自动队形收缩 | ⚠️ 有接口未调用 | ❌ | **详见 S4 节 D1-D4** |
| 故障后拓扑重构 | ✅ | ✅ | **C++ λ₂ 计算错误（见 13.2）** |
| **动力学** |
| Euler 角模型 | ✅ | ✅ | |
| Quaternion 模型 | ✅ | ⚠️ 简化 | C++ 端简化 |
| BEMRotor | ✅ | ⚠️ 简化 | 默认仍用简化 `Rotor`，未自动切换 |
| 风场（稳态 + 湍流） | ✅ | ⚠️ 简化 | |
| **障碍物** |
| AABB / Sphere / Cylinder SDF | ✅ | ✅ | |
| ESDF 软代价场 | ✅ | ✅ | |
| SDFAwareGrid 薄障碍精判 | ✅ | ✅ | |
| 占据栅格 + 膨胀 | ✅ | ✅ | 不支持三轴膨胀 |
| **容错** |
| 故障注入（单旋翼衰减 / 单机退出） | ✅ | ✅ | |
| 在线故障检测（三规则） | ✅ | ✅ | |
| 拓扑重构（λ₂ 最大） | ✅ | ⚠️ | C++ 实现错误 |
| **仿真与可视化** |
| 3D 轨迹图（matplotlib） | ✅ | ⚠️ SVG/PNG | C++ 为静态图 |
| 动态回放 Web（Plotly） | ✅ | ✅ JSON | C++ 输出 replay JSON，前端复用 |
| benchmark 批量评测 | ✅ | ✅ | |
| 自动实验报告（Markdown） | ❌ | ❌ | 未实现 |

### 13.2 已知缺陷清单（已定位）

| 编号 | 模块 | 位置 | 缺陷内容 | 严重度 | 修复建议 |
|:---:|---|---|---|:---:|:---|
| **D1** | 编队包络 | `core/topology.py:149` `cpp/src/topology.cpp:174` | `envelope_radius()` 使用各向同性范数 `max(\|offset\|)`，线性队形横向被高估 | 🔴 | 新增 `envelope_per_axis()`；`_inflate_r` 改用分轴 |
| **D2** | 栅格膨胀 | `simulations/obstacle_scenario.py:402` `cpp/src/obstacle_scenario.cpp:107` | `_inflate_r()` 固定使用 `config.initial_formation` 计算包络，不随队形切换更新 | 🟡 | 改为使用 `topology.current_formation` |
| **D3** | 自动队形收缩 | `core/topology.py:178` | `auto_shrink()` 有定义但**未被任何主流程调用** | 🟡 | 在 `obstacle_scenario.py` 在线循环中接入 |
| **D4** | 分轴包络 | 全项目 | `envelope_per_axis()`（横向/纵向/垂向）完全未实现 | 🟢 | 见 S4 节修复文件清单 |
| **D5** | C++ λ₂ 计算 | `cpp/src/topology.cpp:191-208` | `algebraic_connectivity()` 错误实现为"最小节点度"，非 Laplacian 第二小特征值 | 🔴 | 改为构建 Laplacian 矩阵后求特征值 |
| **D6** | BEMRotor 切换 | `core/drone.py` | `BEMRotor` 已实现，但默认仍使用简化 `Rotor` | 🟡 | 在 `DroneParams` profile 中增加 rotor_model 字段 |
| **D7** | 三轴栅格膨胀 | `core/obstacles.py` `cpp/include/occupancy_grid.hpp` | `OccupancyGrid.inflate()` 只支持标量半径，不支持 `(rx, ry, rz)` | 🟢 | 扩展为椭球结构元膨胀 |
| **D8** | 自动实验报告 | 全项目 | 无自动生成 Markdown/JSON 实验报告能力 | 🟢 | 见 S6 节 |
| **D9** | config 硬编码 | `config.py` | 部分场景参数仍分散在 `main_obstacle()` 等函数中，未完全 config-driven | 🟡 | 逐步迁移到 `SimulationConfig` |

### 13.3 待深入审查模块（尚未逐项对照）

以下模块尚未按照"一步一步对照功能和接口"的深度进行审查，留待后续补充：

| 模块 | 预计审查重点 | 关联文件 |
|---|---|---|
| **控制器三种实现一致性** | PID/Backstepping/SMC 的 `compute_control` 接口是否统一；倾角限幅后推力一致性是否在所有控制器中生效 | `core/controller.py`, `core/smc.py` |
| **WindowReplanner 三级分层** | sliding-window A* → D* Lite → Informed RRT* 的回退链路是否完整；C++ 简化版缺失哪些能力 | `core/planning/replanner.py`, `cpp/src/replanner.cpp` |
| **ESDF 计算正确性** | `compute_esdf()` 自由体素到最近占据体素的距离是否在所有地图边界条件下正确 | `core/planning/esdf.py`, `tests/test_esdf_correct.py` |
| **APF 三档 profile** | conservative/aggressive 档的参数是否被实际加载；编队整体势场是否只叠加不替代 | `core/artificial_potential_field.py`, `core/planning/dual_mode.py` |
| **传感器噪声与延迟** | `RangeSensor6` 的高斯噪声是否按配置注入；C++ 端传感器模型是否与 Python 一致 | `core/sensors.py`, `cpp/src/sensors.cpp` |
| **动态回放数据完整性** | C++ `sim_dynamic_replay` 输出的 replay JSON 是否包含全部必要字段；Python/C++ 回放口径是否一致 | `web/server.py`, `cpp/src/dynamic_main.cpp` |
