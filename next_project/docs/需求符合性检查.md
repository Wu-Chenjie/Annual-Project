# next_project 需求符合性检查

## 检查依据

依据上层文档：`研究计划书.md`。

## 对照结论

| 计划阶段 | 关键要求 | 当前状态 | 说明 |
|---|---|---|---|
| 阶段一 高保真建模 | 统一动力学接口、支持欧拉角与四元数 | 已满足 | `core/drone.py` 同时提供 `Drone` 与 `QuaternionDrone` |
| 阶段一 高保真建模 | 执行机构建模（旋翼/电机/分配） | 已满足 | `core/rotor.py`、`core/allocator.py` 已接入主动力学 |
| 阶段一 高保真建模 | BEM 与简化模型统一切换 | 部分满足 | `BEMRotor` 已实现，当前主流程默认使用简化旋翼 |
| 阶段二 扰动建模 | 稳态风+湍流风场 | 已满足 | `core/wind_field.py` 与仿真主流程已接入 |
| 阶段二 扰动建模 | 标准化扰动复现实验 | 已满足 | `SimulationConfig` 支持可配置随机种子 |
| 阶段三 控制升级 | PID 与 SMC 统一接口 | 已满足 | `core/controller.py` 已支持混合控制 |
| 阶段三 控制升级 | 切换判据与抗抖振 | 部分满足 | 抗抖振已有，自动切换判据仍可继续增强 |
| 阶段四 拓扑协同 | 拓扑接入主流程与平滑切换 | 已满足 | `FormationTopology` 已接入仿真，并支持计划触发切换 |
| 阶段五 规范评测 | 批量评测与结果导出 | 已满足 | `simulations/benchmark.py` 输出 `outputs/benchmark_results.json` |

## 本轮优化项

1. 将仿真重构为配置驱动，减少硬编码参数。
2. 采用预分配数组替代循环内 append，降低内存分配开销。
3. 通过领航机平滑机动参数与航点切换半径优化，显著降低编队误差。
4. 增加主机加速度低通滤波，减小前馈抖动。
5. 增加批量评测脚本，支持多随机种子复现实验与 JSON 导出。

## 优化后结果

主入口 `main.py` 实测（默认配置）：

- 运行耗时约 `2.3s`
- 三架从机平均误差分别约 `0.1805m`、`0.2110m`、`0.1999m`
- 全部小于 `0.3m` 阈值

批量评测 `simulations/benchmark.py`（5 组随机种子）：

- 平均运行时间约 `2.591s`
- 从机平均误差均值约 `0.2027m`、`0.2022m`、`0.1991m`
- `0.3m` 阈值全通过

## 中期需求补充检查（2026-05-13）

| 需求 | 当前状态 | 证据 |
|---|---|---|
| 输出测试结果图片和 Markdown 报告 | 已满足 | `experiments/result_reporter.py`、`experiments/report_cpp_results.py`、`outputs/*/report_figures/` |
| 报告包含路径规划路线参数、飞行参数和事件 | 已满足 | `report.md` / `cpp_report.md` 包含路径、规划器耗时、飞行参数、误差、碰撞、航点、重规划事件 |
| 支持预设场景显示 | 已满足 | `config.py`、`experiments/scenario_registry.py`、`web/server.py` |
| 规划模式对比并说明最优原因 | 已满足 | `experiments/planner_comparison.py` 输出 `summary.csv`、`report.md` 和 `规划模式对比.png` |
| C++ 仿真也能输出结果报告 | 已满足 | `sim_warehouse` + `experiments.run_cpp_report` |
| C++ 数据缺失问题改善 | 已满足 | C++ JSON 增加 timing、obstacle_model、planning_events、waypoint_events、formation_adaptation_events |
| 编队调控机制 | 已满足 | `core/formation_adaptation.py`、`formation_adaptation_events`、消融变体 |
| 前瞻窗口提前触发队形变换 | 已满足 | `formation_lookahead_enabled`、`lookahead_reference_blocked` 事件 |
| RRT 辅助局部假死路逃逸 | 已满足 | `rrt_escape_attempt_count`、`rrt_escape_accepted_count` |
| 中期验收证据 | 已满足 | `python -m pytest -q` 为 `170 passed, 3 skipped`；C++ 报告测试 `6 passed`；跨线回归 `3 passed` |

边界说明：

- 当前仍是仿真与工程验证，不是实机飞行认证。
- C++ 是可运行子集，不宣称完整等价 Python 主线。
- 轨迹优化为 `minimum_jerk` / `min_snap_proxy` 工程原型，不宣称完整 GCOPTER/MINCO。
